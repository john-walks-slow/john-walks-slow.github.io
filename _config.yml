# Site settings
title: 小齐慢慢前进 | John Walks Slow
title-short: 小齐慢慢前进!
SEOTitle: 小齐慢慢前进 | John Walks Slow
header-img: img/fun-bg.jpg
email: huxpro@gmail.com
description: '小齐的Blog | 没用的玩、没结果的思考、记不住的记录'
keyword: '小齐, 一倍速小齐, JohnnRen, 小齐的博客, John Walks Slow, 博客, 个人网站'
url: 'https://john-walks-slow.github.io/' # your host, for absolute URL
baseurl: '' # for example, '/blog' if your blog hosted on 'host/blog'

# Publish posts or collection documents with a future date.
future: true

# SNS settings
RSS: true
weibo_username: JohnnRen
zhihu_username: john-walks-slow
github_username: john-walks-slow
facebook_username: john.walks.slow
douban_username: john-walks-slow
streetvoice_username: john_walks_slow
wyy_username: '125143088'
#linkedin_username:  firstname-lastname-idxxxx

# Build settings
# from 2016, 'pygments' is unsupported on GitHub Pages. Use 'rouge' for highlighting instead.
highlighter: rouge
permalink: pretty
exclude: ['less', 'node_modules', 'Gruntfile.js', 'package.json', 'README.md', 'README.zh.md']
anchorjs: true # if you want to customize anchor. check out line:181 of `post.html`
# If you have timezone issue (e.g. #68) in China, uncomment to use this:
#timezone: CN

# Gems
# from PR#40, to support local preview for Jekyll 3.0
# make sure you have this gem installed
# `$ gem install jekyll-paginate`
plugins: [jekyll-paginate-v2]
pagination:
  # Site-wide kill switch, disabled here it doesn't run at all
  enabled: true
  title: ':title'
  permalink: '/page/:num/'
  # Optional, defines the field that the posts should be sorted on (omit to default to 'date')
  sort_field: 'date'
  # Optional, sorts the posts in reverse order (omit to default decending or sort_reverse: true)
  sort_reverse: true
# Markdown settings
# replace redcarpet to kramdown,
# although redcarpet can auto highlight code, the lack of header-id make the catalog impossible, so I switch to kramdown
# document: http://jekyllrb.com/docs/configuration/#kramdown
markdown: kramdown
kramdown:
  input: GFM # use Github Flavored Markdown !important
  syntax_highlighter_opts:
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

# Disqus settings
disqus_username: john-walks-slow

# Netease settings
netease_comment: false

# Analytics settings
# Baidu Analytics
# ba_track_id: [your track id]

# Sidebar settings
sidebar: true # whether or not using Sidebar.
sidebar-about-description: '我是小齐。<br>我有很多的问题。<br>我喜欢玩音乐，玩电脑和想事情！'
avatar: /img/avatar.jpg # use absolute URL, seeing it's used in both `/` and `/about/`

# Featured Tags
featured-tags: true # whether or not using Feature-Tags
featured-condition-size: 0 # A tag will be featured if the size of it is more than this condition value

# Progressive Web Apps
chrome-tab-theme-color: '#000000'
service-worker: true

# MathJax rendering for layout:page (e.g. post preview)
page-mathjax: false

# # Friends
# friends: []

defaults:
  - scope:
      path: ''
      type: 'posts'
    values:
      layout: post
      author: '小齐'
      header-img: 'img/fun-bg.jpg'
      tags:
        - 杂文
  - scope:
      path: '_posts/玩'
      type: 'posts'
    values:
      layout: post
      author: '小齐'
      header-img: 'img/fun-bg.jpg'
      categories:
        - 玩
  - scope:
      path: '_posts/思考'
      type: 'posts'
    values:
      layout: post
      author: '小齐'
      header-img: 'img/fun-bg.jpg'
      categories:
        - 思考
  - scope:
      path: '_posts/记录'
      type: 'posts'
    values:
      layout: post
      author: '小齐'
      header-img: 'img/fun-bg.jpg'
      categories:
        - 记录
